---
layout: post
comments: true
sitemap:

title: "[LOGIC] 진리표 증명"
excerpt: "직접 진리표와 간접 진리표"

date: 2026-05-05
last_modified_at: 2026-05-05

categories: [LOGIC]
tags: [LOGIC]
---

<!-- markdownlint-disable MD007 MD010 MD025 MD033 -->

# 진리표 증명

명제 논리에서 어떤 논증이 **타당한지 부당한지**를 판별하기 위해 **진리표**를 사용하는 방법

1. **직접 진리표**
2. **간접 진리표**

## 1. 직접 진리표

### 1.1. 개념

직접 진리표: 논증에 등장하는 모든 단순명제의 가능한 진리값 조합을 전부 나열한 뒤, **전제가 모두 참인 경우에 결론이 거짓인 행이 있는지** 확인한다.

-> **전수조사형**

### 1.2. 절차

1. 논증을 기호화한다.
    - 전제와 전제 사이는 `,`
    - 전제와 결론 사이는 `/`
    - 예: `J⊃E, ∼J / ∼E`
2. 각 명제 아래에 진리표를 작성한다.
3. 진리표에서
    - **전제가 모두 참이고 결론이 거짓인 줄이 있으면** → **부당**
    - **그런 줄이 하나도 없으면** → **타당**

### 1.3. 예시

#### 1.3.1. `J⊃E, ∼J / ∼E`

| J | E | J⊃E | ∼J | ∼E |
| --- | --- | --- | --- | --- |
| T | T | T | F | F |
| T | F | F | F | T |
| <font color="#ff0000">F</font> | <font color="#ff0000">T</font> | <font color="#ff0000">T</font> | <font color="#ff0000">T</font> | <font color="#ff0000">F</font> |
| F | F | T | T | T |

전제가 모두 참인 행을 찾는다.

| J | E | J⊃E | ∼J | ∼E |
| --- | --- | --- | --- | --- |
| F | T | T | T | F |

이 행에서

- 전제 `J⊃E` = 참
- 전제 `∼J` = 참
- 결론 `∼E` = 거짓

따라서 **전제가 모두 참인데 결론이 거짓인 경우가 존재**한다.

> `J⊃E, ∼J / ∼E`는 **부당한 논증**이다.

##### 1.3.2. `A⊃∼R, ∼R⊃K / A⊃K`

| A | R | K | ∼R | A⊃∼R | ∼R⊃K | A⊃K |
| --- | --- | --- | --- | --- | --- | --- |
| T | T | T | F | F | T | T |
| T | T | F | F | F | T | F |
| T | F | T | T | T | T | T |
| T | F | F | T | T | F | F |
| F | T | T | F | T | T | T |
| F | T | F | F | T | T | T |
| F | F | T | T | T | T | T |
| F | F | F | T | T | F | T |

전제 `A⊃∼R`, `∼R⊃K`가 모두 참인 행을 확인한다.

| A | R | K | A⊃∼R | ∼R⊃K | A⊃K |
| --- | --- | --- | --- | --- | --- |
| T | F | T | T | T | T |
| F | T | T | T | T | T |
| F | T | F | T | T | T |
| F | F | T | T | T | T |

전제가 모두 참인 모든 행에서 결론 `A⊃K`도 참이다.

> `A⊃∼R, ∼R⊃K / A⊃K`는 **타당한 논증**이다.

#### 1.3.3. `~(X∙~Y), ~(Y∙~X) / Y∨X`

| X | Y | ~Y | X∙~Y | ~(X∙~Y) | ~X | Y∙~X | ~(Y∙~X) | Y∨X |
| - | - | -- | ---- | ------- | -- | ---- | ------- | --- |
| T | T | F  | F    | T       | F  | F    | T       | T   |
| T | F | T  | T    | F       | F  | F    | T       | T   |
| F | T | F  | F    | T       | T  | T    | F       | T   |
| F | F | T  | F    | T       | T  | F    | T       | F   |

전제들이 모두 참인 행:

| X | Y | ~(X∙~Y) | ~(Y∙~X) | Y∨X |
| - | - | ------- | ------- | --- |
| T | T | T       | T       | T   |
| F | F | T       | T       | F   |

전제가 모두 참인데 결론이 거짓인 행이 있다.

> **부당**

#### 1.3.4. `~(H∙O), O∨~H / ~H`

| H | O | H∙O | ~(H∙O) | ~H | O∨~H |
| - | - | --- | ------ | -- | ---- |
| T | T | T   | F      | F  | T    |
| T | F | F   | T      | F  | F    |
| F | T | F   | T      | T  | T    |
| F | F | F   | T      | T  | T    |

전제들이 모두 참인 행:

| H | O | ~(H∙O) | O∨~H | ~H |
| - | - | ------ | ---- | -- |
| F | T | T      | T    | T  |
| F | F | T      | T    | T  |

전제가 모두 참인 모든 행에서 결론도 참이다.

> **타당**

#### 1.3.5. `D≡E, F∨~E / F⊃D`

| D | E | F | D≡E | ~E | F∨~E | F⊃D |
| - | - | - | --- | -- | ---- | --- |
| T | T | T | T   | F  | T    | T   |
| T | T | F | T   | F  | F    | T   |
| T | F | T | F   | T  | T    | T   |
| T | F | F | F   | T  | T    | T   |
| F | T | T | F   | F  | T    | F   |
| F | T | F | F   | F  | F    | T   |
| F | F | T | T   | T  | T    | F   |
| F | F | F | T   | T  | T    | T   |

전제들이 모두 참인 행:

| D | E | F | D≡E | F∨~E | F⊃D |
| - | - | - | --- | ---- | --- |
| T | T | T | T   | T    | T   |
| F | F | T | T   | T    | F   |
| F | F | F | T   | T    | T   |

전제가 모두 참인데 결론이 거짓인 행이 있다.

> **부당**

#### 1.3.6. `B≡(C∨D), ~D∨C / B⊃C`

| B | C | D | C∨D | B≡(C∨D) | ~D | ~D∨C | B⊃C |
| - | - | - | --- | ------- | -- | ---- | --- |
| T | T | T | T   | T       | F  | T    | T   |
| T | T | F | T   | T       | T  | T    | T   |
| T | F | T | T   | T       | F  | F    | F   |
| T | F | F | F   | F       | T  | T    | F   |
| F | T | T | T   | F       | F  | T    | T   |
| F | T | F | T   | F       | T  | T    | T   |
| F | F | T | T   | F       | F  | F    | T   |
| F | F | F | F   | T       | T  | T    | T   |

전제들이 모두 참인 행:

| B | C | D | B≡(C∨D) | ~D∨C | B⊃C |
| - | - | - | ------- | ---- | --- |
| T | T | T | T       | T    | T   |
| T | T | F | T       | T    | T   |
| F | F | F | T       | T    | T   |

전제가 모두 참인 모든 행에서 결론도 참이다.

> **타당**

#### 1.3.7. `B⊃(O∨R), ~(O∨~B) / B⊃R`

| B | O | R | O∨R | B⊃(O∨R) | ~B | O∨~B | ~(O∨~B) | B⊃R |
| - | - | - | --- | ------- | -- | ---- | ------- | --- |
| T | T | T | T   | T       | F  | T    | F       | T   |
| T | T | F | T   | T       | F  | T    | F       | F   |
| T | F | T | T   | T       | F  | F    | T       | T   |
| T | F | F | F   | F       | F  | F    | T       | F   |
| F | T | T | T   | T       | T  | T    | F       | T   |
| F | T | F | T   | T       | T  | T    | F       | T   |
| F | F | T | T   | T       | T  | T    | F       | T   |
| F | F | F | F   | T       | T  | T    | F       | T   |

전제들이 모두 참인 행:

| B | O | R | B⊃(O∨R) | ~(O∨~B) | B⊃R |
| - | - | - | ------- | ------- | --- |
| T | F | T | T       | T       | T   |

전제가 모두 참인 모든 행에서 결론도 참이다.

> **타당**

## 2. 간접 진리표

### 2.1. 개념

간접 진리표: 모든 경우를 전부 나열하지 않고, 먼저 **논증이 부당하다고 가정**해서 출발한다.

즉, **전제는 모두 참, 결론은 거짓**이라고 놓고, 그 가정이 **모순 없이 유지될 수 있는지**를 본다.

-> **반례 가능성 검사형**

### 2.2. 절차

1. 논증을 기호화하여 한 줄로 쓴다.
2. 이 논증이 부당하다고 가정한다.  
    - 전제들은 모두 참, 결론은 거짓으로 놓고 각 명제의 **주논리연결사** 아래에 진리값을 표시한다.
3. 계산을 진행했을 때
    - **모순 없이 끝까지 가능하면** → **부당**
    - **어딘가에서 모순이 생기면** → **타당**

### 2.3. 예시

## 2.3.1. `∼C⊃(D∨R), ∼B / R⊃C`

결론 `R⊃C`가 거짓이려면:

```text
R = T, C = F
```

전제 확인:

```text
∼C = T
D∨R = T
∼C⊃(D∨R) = T
∼B = T 이므로 B = F
```

모순 없음.

> **부당**

---

## 2.3.2. `G⊃(H∨D), H⊃K, G / ∼D⊃K`

결론 `∼D⊃K`가 거짓이려면:

```text
∼D = T, K = F
D = F
```

전제 `G = T`.

전제 `G⊃(H∨D)`가 참이고 `G = T`이므로:

```text
H∨D = T
```

그런데 `D = F`이므로:

```text
H = T
```

전제 `H⊃K`에서 `H = T`이면:

```text
K = T
```

하지만 처음에 `K = F`였다.

```text
K = T 이면서 K = F
```

모순.

> **타당**

---

## 2.3.3. `W⊃P, K⊃P / W⊃Q`

결론 `W⊃Q`가 거짓이려면:

```text
W = T, Q = F
```

전제 `W⊃P`가 참이고 `W = T`이므로:

```text
P = T
```

전제 `K⊃P`는 `P = T`이면 항상 참이다.

예를 들어:

```text
W = T, P = T, K = F, Q = F
```

모순 없음.

> **부당**

---

## 2.3.4. `D∙(E∨F), (D∙F)⊃∼(R∨S), (~R∨~S)⊃~(D∙E) / R≡S`

결론 `R≡S`가 거짓이려면:

```text
R과 S의 진리값이 다름
```

즉 다음 중 하나다.

```text
R = T, S = F
또는
R = F, S = T
```

두 경우 모두:

```text
~R∨~S = T
```

전제 `(~R∨~S)⊃~(D∙E)`에서:

```text
~(D∙E) = T
D∙E = F
```

그런데 전제 `D∙(E∨F)`에서:

```text
D = T
E∨F = T
```

이제 `D∙E = F`이고 `D = T`이므로:

```text
E = F
```

그러면 `E∨F = T`가 되려면:

```text
F = T
```

따라서:

```text
D = T, F = T
D∙F = T
```

전제 `(D∙F)⊃∼(R∨S)`에서:

```text
∼(R∨S) = T
R∨S = F
R = F, S = F
```

하지만 결론 거짓 가정에서는 `R`과 `S` 중 하나는 참이어야 했다.

모순.

> **타당**

## 2.3.5. `(A∨B)⊃C, C⊃(B∨R), A⊃(~Q⊃B), (Q⊃A)⊃~R / B≡C`

결론 `B≡C`가 거짓이려면:

```text
B와 C의 진리값이 다름
```

반례:

```text
B = F, C = T
```

전제 `C⊃(B∨R)`에서 `C = T`이므로:

```text
B∨R = T
```

`B = F`이므로:

```text
R = T
```

전제 `(Q⊃A)⊃~R`에서 `R = T`이므로:

```text
~R = F
```

이 조건문이 참이려면 앞부분이 거짓이어야 한다.

```text
Q⊃A = F
```

따라서:

```text
Q = T, A = F
```

전제 `(A∨B)⊃C`는:

```text
A∨B = F
C = T
```

앞이 거짓이므로 참.

전제 `A⊃(~Q⊃B)`는 `A = F`이므로 참.

모순 없음.

> **부당**

---

## 2.3.6. `(P∨Q)⊃(R∙S), (~P∨~Q)⊃E / (~R∨~S)⊃E`

결론 `(~R∨~S)⊃E`가 거짓이려면:

```text
~R∨~S = T
E = F
```

전제 `(~P∨~Q)⊃E`에서 `E = F`이므로 이 전제가 참이려면:

```text
~P∨~Q = F
```

따라서:

```text
P = T, Q = T
```

그러면:

```text
P∨Q = T
```

전제 `(P∨Q)⊃(R∙S)`에서:

```text
R∙S = T
R = T, S = T
```

그런데 처음에 `~R∨~S = T`였다.

`R = T, S = T`이면:

```text
~R∨~S = F
```

모순.

> **타당**

## 2.3.7. `M⊃N, ~O∨P, (N∨P)⊃J, ~J / ~(M∨O)`

결론 `~(M∨O)`가 거짓이려면:

```text
M∨O = T
```

전제 `~J`에서:

```text
J = F
```

전제 `(N∨P)⊃J`에서 `J = F`이므로 이 전제가 참이려면:

```text
N∨P = F
```

따라서:

```text
N = F, P = F
```

전제 `M⊃N`에서 `N = F`이므로 참이려면:

```text
M = F
```

전제 `~O∨P`에서 `P = F`이므로 참이려면:

```text
~O = T
O = F
```

따라서:

```text
M = F, O = F
M∨O = F
```

하지만 결론 거짓 가정에서는 `M∨O = T`였다.

모순.

> **타당**